# How to build docker container

```bash
docker compose up -d --build
```

# Web interface

```bash
localhost:1080
```

# wp usecase

```bash
host : mailcatcher
port: 1025
TLS Off
login Off
```